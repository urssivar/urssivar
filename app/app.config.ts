export default defineAppConfig({
    ui: {
        tooltip: {
            slots: {
                content: 'data-[state=instant-open]:animate-[scale-in_100ms_ease-out]'
            }
        }
    },
});
